// Map of each itemID in the basket (key) with the order quantity (value)
// let itemMap = {};
// for(const basketItem of items) {
//   for(const itemID in basketItem.itemIDs) {
//     itemMap[itemID] = basketItem.quantity;
//   }
// }
